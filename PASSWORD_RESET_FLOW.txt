╔════════════════════════════════════════════════════════════════════════════╗
║                   PASSWORD RESET FLOW - VISUAL DIAGRAM                     ║
╚════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│                              USER JOURNEY                                   │
└─────────────────────────────────────────────────────────────────────────────┘


   ┌─────────────┐
   │ Login Page  │  User clicks "Forgot password?" link
   │             │  URL: /auth/login/
   └──────┬──────┘
          │
          ▼
   ┌─────────────────────┐
   │ Password Reset Form │  Enter email address
   │                     │  URL: /auth/password-reset/
   │  [Email Input]      │  View: CustomPasswordResetView
   │  [Submit Button]    │
   └──────┬──────────────┘
          │
          ▼
   ┌───────────────────────┐
   │ Password Reset Done   │  "Check your email" message
   │                       │  URL: /auth/password-reset/done/
   │  ✅ Email Sent!       │  View: PasswordResetDoneView
   │  Check your inbox...  │
   └───────────────────────┘
          │
          ├────────────────────────────┐
          │                            │
   [USER INBOX]                  [CONSOLE/TERMINAL]
          │                            │
          ▼                            ▼
   ┌──────────────────┐      ┌─────────────────────┐
   │  📧 Email Arrives │      │  Email printed to   │
   │                  │      │  console (DEBUG=True)│
   │  Subject:        │      │                     │
   │  "Reset Your     │      │  Copy link and open │
   │   Password"      │      │  in browser         │
   │                  │      └─────────────────────┘
   │  [Reset Button]  │
   │  Or copy link... │
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────────────┐
   │ Password Reset Confirm   │  Set new password
   │                          │  URL: /auth/password-reset-confirm/<token>/
   │  [New Password]          │  View: CustomPasswordResetConfirmView
   │  [Confirm Password]      │
   │  [Submit Button]         │
   │                          │
   │  Token validated ✅      │
   └────────┬─────────────────┘
            │
            ▼
   ┌──────────────────────────┐
   │ Password Reset Complete  │  Success message
   │                          │  URL: /auth/password-reset-complete/
   │  ✅ Success!             │  View: PasswordResetCompleteView
   │  Password updated        │
   │                          │
   │  [Continue to Login]     │
   └────────┬─────────────────┘
            │
            ▼
   ┌─────────────┐
   │ Login Page  │  Log in with new password
   │             │  URL: /auth/login/
   └─────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                           ERROR SCENARIOS                                   │
└─────────────────────────────────────────────────────────────────────────────┘

Invalid/Expired Token:
   ┌──────────────────────────┐
   │ Password Reset Confirm   │
   │                          │
   │  ❌ Invalid Link         │  Token expired or already used
   │  This link has expired   │  Show error message
   │                          │
   │  [Request New Link]      │  Returns to /auth/password-reset/
   └──────────────────────────┘


Email Not Found:
   ┌─────────────────────┐
   │ Password Reset Form │
   │                     │  Still shows success message
   │  ✅ If that email   │  (security: don't reveal if email exists)
   │  exists, we sent... │
   └─────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                          TECHNICAL FLOW                                     │
└─────────────────────────────────────────────────────────────────────────────┘

1. Request Reset
   ┌──────────────────────────────────────────────────────────┐
   │  User submits email → CustomPasswordResetView            │
   │  ↓                                                        │
   │  Django generates secure token                           │
   │  ↓                                                        │
   │  Creates reset URL with UID and token                    │
   │  ↓                                                        │
   │  Sends email (console in dev, SMTP in prod)              │
   │  ↓                                                        │
   │  Shows success message (toast notification)              │
   │  ↓                                                        │
   │  Redirects to "done" page                                │
   └──────────────────────────────────────────────────────────┘

2. Verify Token
   ┌──────────────────────────────────────────────────────────┐
   │  User clicks link → CustomPasswordResetConfirmView       │
   │  ↓                                                        │
   │  Django validates token (check expiry, validity)         │
   │  ↓                                                        │
   │  IF valid: Show password form                            │
   │  IF invalid: Show error message                          │
   └──────────────────────────────────────────────────────────┘

3. Reset Password
   ┌──────────────────────────────────────────────────────────┐
   │  User submits new password → Form validation             │
   │  ↓                                                        │
   │  Check password requirements (8+ chars, not all numeric) │
   │  ↓                                                        │
   │  Hash and save new password                              │
   │  ↓                                                        │
   │  Invalidate token (one-time use)                         │
   │  ↓                                                        │
   │  Show success message (toast notification)               │
   │  ↓                                                        │
   │  Redirect to "complete" page                             │
   └──────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                            SECURITY LAYERS                                  │
└─────────────────────────────────────────────────────────────────────────────┘

Token Generation:
   [User Email] → SHA256 Hash → [Random Salt] → [Timestamp] → [Secure Token]
                    ↓
              Valid for 24 hours
                    ↓
              One-time use only

Email Verification:
   - Checks if user exists
   - No information leakage
   - Same message for existing/non-existing emails

Password Validation:
   ✅ Minimum 8 characters
   ✅ Cannot be entirely numeric
   ✅ Must be different from old password
   ✅ Confirm password must match

URL Security:
   - Token encoded in URL
   - CSRF protection
   - HTTPS recommended
   - No password in URL


┌─────────────────────────────────────────────────────────────────────────────┐
│                          FILES INVOLVED                                     │
└─────────────────────────────────────────────────────────────────────────────┘

Views:
   users/views.py
      ├── CustomPasswordResetView
      └── CustomPasswordResetConfirmView

URLs:
   users/urls.py
      ├── /password-reset/
      ├── /password-reset/done/
      ├── /password-reset-confirm/<uidb64>/<token>/
      └── /password-reset-complete/

Templates:
   users/templates/users/
      ├── password_reset_form.html       (step 1)
      ├── password_reset_done.html       (step 2)
      ├── password_reset_confirm.html    (step 3)
      ├── password_reset_complete.html   (step 4)
      ├── password_reset_email.html      (email body)
      └── password_reset_subject.txt     (email subject)

Configuration:
   hcot/settings.py
      ├── EMAIL_BACKEND
      ├── EMAIL_HOST
      ├── EMAIL_PORT
      ├── EMAIL_USE_TLS
      ├── EMAIL_HOST_USER
      ├── EMAIL_HOST_PASSWORD
      └── DEFAULT_FROM_EMAIL

Environment:
   .env
      └── [All email settings stored here]


┌─────────────────────────────────────────────────────────────────────────────┐
│                         TESTING CHECKLIST                                   │
└─────────────────────────────────────────────────────────────────────────────┘

Development (DEBUG=True):
   ☐ Start server
   ☐ Go to login page
   ☐ Click "Forgot password?"
   ☐ Enter test email
   ☐ Check console for email
   ☐ Copy reset link
   ☐ Open link in browser
   ☐ Set new password
   ☐ Verify login works

Production (DEBUG=False):
   ☐ Configure email provider
   ☐ Add credentials to .env
   ☐ Test with real email
   ☐ Check inbox (and spam)
   ☐ Click email link
   ☐ Set new password
   ☐ Verify login works
   ☐ Test expired token
   ☐ Test invalid token
   ☐ Test password validation


┌─────────────────────────────────────────────────────────────────────────────┐
│                    ENVIRONMENT VARIABLES SUMMARY                            │
└─────────────────────────────────────────────────────────────────────────────┘

Required for Production:
   EMAIL_HOST=smtp.gmail.com
   EMAIL_PORT=587
   EMAIL_USE_TLS=True
   EMAIL_HOST_USER=your-email@gmail.com
   EMAIL_HOST_PASSWORD=your-app-password
   DEFAULT_FROM_EMAIL=noreply@yourdomain.com
   SERVER_EMAIL=noreply@yourdomain.com

Optional:
   DEBUG=False
   SECRET_KEY=your-secret-key
   ALLOWED_HOSTS=yourdomain.com


═══════════════════════════════════════════════════════════════════════════════
                          🎉 SYSTEM READY TO USE! 🎉
═══════════════════════════════════════════════════════════════════════════════
