# ==============================================================================
# DJANGO SETTINGS
# ==============================================================================
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# ==============================================================================
# GOOGLE OAUTH SETTINGS
# ==============================================================================
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# ==============================================================================
# EMAIL SETTINGS (For Password Reset & Notifications)
# ==============================================================================
# Note: In DEBUG mode, emails are printed to console by default
# For production, set DEBUG=False and configure the settings below

# SMTP Email Configuration (Required for production)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password-here
DEFAULT_FROM_EMAIL=noreply@yourdomain.com
SERVER_EMAIL=noreply@yourdomain.com

# For Gmail:
# 1. Enable 2-factor authentication
# 2. Generate an "App Password" at: https://myaccount.google.com/apppasswords
# 3. Use the generated app password as EMAIL_HOST_PASSWORD

# For other email providers:
# - SendGrid: EMAIL_HOST=smtp.sendgrid.net, EMAIL_PORT=587
# - Mailgun: EMAIL_HOST=smtp.mailgun.org, EMAIL_PORT=587
# - AWS SES: EMAIL_HOST=email-smtp.us-east-1.amazonaws.com, EMAIL_PORT=587
# - Outlook/Hotmail: EMAIL_HOST=smtp-mail.outlook.com, EMAIL_PORT=587

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================
# Choose your database engine: "sqlite3" (default) or "postgresql"
# SQLite is perfect for development, PostgreSQL is recommended for production

# Database Engine Selection
# Leave as sqlite3 for development (no other settings needed)
# Change to postgresql for production (then configure settings below)
DATABASE_ENGINE=sqlite3

# ------------------------------------------------------------------------------
# PostgreSQL Settings (Only needed if DATABASE_ENGINE=postgresql)
# ------------------------------------------------------------------------------
# Uncomment and fill these out when using PostgreSQL:
# Requires: pip install psycopg2-binary

# DATABASE_NAME=hcot_db
# DATABASE_USER=postgres
# DATABASE_PASSWORD=your-secure-password-here
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_CONNECT_TIMEOUT=10

# PostgreSQL Setup Instructions:
# 1. Install PostgreSQL: https://www.postgresql.org/download/
# 2. Install Python driver: pip install psycopg2-binary
# 3. Create database:
#    - Open psql terminal: psql -U postgres
#    - Run: CREATE DATABASE hcot_db;
#    - Run: CREATE USER your_user WITH PASSWORD 'your_password';
#    - Run: GRANT ALL PRIVILEGES ON DATABASE hcot_db TO your_user;
# 4. Update the DATABASE_* variables above
# 5. Change DATABASE_ENGINE to postgresql
# 6. Run migrations: python manage.py migrate
